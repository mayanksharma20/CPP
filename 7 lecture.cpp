#include<iostream>
#include<math.h>
using namespace std;
int main()
{
    /*
    //reverse of a number
    int n;
    cout<<"Read_";
    cin>>n;
    int rem;
    int rev=0;
    while(n!=0){
        rem=n%10;
        if((rev>INT_MAX/10)||(rev<INT_MIN/10)){
            return 0;
        }
        rev=(rev*10)+rem;
        n=n/10;
    }
    cout<<rev;
    

   //compliment of a number;
   int n;
   cin>>n;
   int m=n;
   int mask=0;

   //edge case
   if(n==0)
    return 1;

    while(m!=0){
        mask=(mask<<1)|1;
        m=m>>1;
    }
    int ans=(~n)&mask;
    cout<<ans;
    
   int n;
   cin>>n;
   int ans=0;
   int count=0;
   for(int i=0;i<=n;i++){
        ans=pow(2,i);
        
        if(ans==n)
        count++;
    }

    if(count==1)
    cout<<"true";
    else
    cout<<"false";
    */
   
        
        
   
}